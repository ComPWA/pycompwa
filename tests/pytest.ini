[pytest]
testpaths = .
addopts =
    --cov=pycompwa
    --cov-config=.coveragerc
    --durations=0
    --cov-report=html
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    --durations=0
    --cov-report=html
